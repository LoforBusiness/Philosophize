import type { ImageSourcePropType } from 'react-native';

// Parchment / classical backgrounds shown behind lesson cards.
//
// HOW TO ADD YOURS
//   1. Save your images into  assets/backgrounds/  (PNG/JPG, portrait, ~1080×1920).
//   2. Uncomment / add a require() line for each below.
//
// Until at least one image is registered here, lessons use a built-in
// procedural parchment (see LessonBackground.tsx), so the screen still looks good.
export const LESSON_BACKGROUNDS: ImageSourcePropType[] = [
  // require('@/assets/backgrounds/bg-1.png'),
  // require('@/assets/backgrounds/bg-2.png'),
  // require('@/assets/backgrounds/bg-3.png'),
  // require('@/assets/backgrounds/bg-4.png'),
  // require('@/assets/backgrounds/bg-5.png'),
];
